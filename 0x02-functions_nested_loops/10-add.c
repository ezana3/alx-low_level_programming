#include "main.h"
#include "stdio.h"

/**
 * add - adds.
 * main - function.
 * Return - Always 0;
 */

int main(void)
{
	int add(int x, int y)
	{
		int n;

		n = add(x, y);
		printf("%d\n", n);
		return (0);
	}
}
