#include "main.h"

/**
 * _islower -> checks if the character is lowercase.
 * @c: a charcter argument
 * Return: returns 1 or 0 depending on condition0;
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
